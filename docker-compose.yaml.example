
services:
  # Honeygain Autoclaim
  crontab:
      container_name: autoclaim
      build:
          context: .
          dockerfile: Dockerfile
      environment:
          - TZ=Asia/Ho_Chi_Minh
          - TOKEN=token
      restart: always
